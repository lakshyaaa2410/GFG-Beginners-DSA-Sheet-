	int findMaximum(int arr[], int n) {
        int maxElement = 0;
        
        for (int i = 0; i < n; i++)
            if (arr[i] > maxElement) 
                maxElement = arr[i];
        
        return maxElement;
	}
