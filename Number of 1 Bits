    int setBits(int N) {
        
        int numOfSetBits = 0;
        while (N > 0)
        {
            if (N & 1)
                numOfSetBits++;
            N = N >> 1;
        }
        
        return numOfSetBits;
    }
